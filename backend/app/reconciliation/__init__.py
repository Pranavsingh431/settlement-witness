"""The deterministic reconciliation baseline.

Matches on exact references only. No amount similarity, no timestamp proximity,
no text comparison, no model. What it cannot justify from a direct reference and
the verifier contract, it declines to resolve.
"""
